<%= form_with model: @data_list, scope: :data_list, url: { action: 'update' } do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= render partial: 'com/parameter/item', locals: { model: @data_list, f: f } %>
  <%= f.submit %>
<% end %>

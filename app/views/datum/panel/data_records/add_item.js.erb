<% form_object = form_object(@data_record, can: { wrap_id: true }) %>

var template = '<%= j(render partial: 'item_form', locals: { f: form_object }) %>'
$('#parameters_<%= params[:index].to_i - 1 %>').after(template);

<%= form_with theme: 'search', wrap: { all: 'inline field', input: 'field', submit: 'field' } do |f| %>
  <div class="fields">
    <% @data_record.parameters.keys.each do |extra| %>
      <%= f.text_field extra, placeholder: extra %>
    <% end %>
    <%= f.submit %>
  </div>
<% end %>


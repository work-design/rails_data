var tl_id = '#done_' + '<%= @table_list.id %>';
var tl_obj = $(tl_id);
tl_obj.html('<%= j(render 'process') %>');

$('.ui.progress').progress({
  label: 'percent',
  text : {
    percent : 'working'
  }
});
<section>

<ul class="breadcrumb">
  <li><%= link_to 'back', :back %></li>
</ul>

<div class="page-header">
  <h1>Preview</h1>
</div>

<div class="pagination pagination-centered">
  <ul>
    <% @table_lists.each_with_index do |table, index| %>
      <li><%= link_to index+1, report_list_table_list_path(table.report_list, table) %></li>
    <% end %>
  </ul>
</div>

<%= render partial: 'table_lists/table', locals: { table_list: @table_lists.first } %>

</section>
<% form_object = default_form_object(@purchase, on: { wrapper_id: true }) %>

var template = '<%= j(render partial: 'item_form', locals: { f: form_object }) %>'
$('#purchase_purchase_items_<%= params[:index].to_i - 1 %>').after(template);

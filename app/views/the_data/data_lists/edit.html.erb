<div>

  <div class="ui segment breadcrumb">
    <%= link_to 'Back', data_lists_path, class: 'section' %>
    <div class="divider"> / </div>
    <div class="active section">Edit</div>
  </div>

  <div class="ui segment">
    <%= form_with model: @data_list, scope: :data_list, url: data_list_path(@data_list), locale: true do |f| %>
      <%= f.text_field :title, label: 'Title' %>
      <%= f.text_area :comment, label: 'Comment' %>
      <%= f.text_field :data_table, label: 'DataTable' %>
      <%= f.text_field :export_excel %>
      <%= f.text_field :export_pdf %>
      <% @data_list.form_parameters.each do |parameter| %>
      <%= f.fields :parameters, model: parameter, index: 0, on: {wrapper_all: false, label: false, placeholder: true}, css: {wrapper_input: 'two wide field'} do |pf| %>
        <div class="inline fields" id="purchase_purchase_items_0">
          <%= f.label :form_parameters %>
          <%= pf.text_field :key, readonly: true %>
          <%= pf.select :value, options_for_select(TheData.config.mapping.keys, parameter.value), class: 'ui dropdown selection' %>
          <div class="field">
            <%= link_to add_item_data_lists_path(index: 1), remote: true do %>
              <i class="plus circle icon"></i>
            <% end %>
          </div>
        </div>
      <% end %>
      <% end %>
      <%= f.submit %>
    <% end %>
  </div>

</div>